There is an undirected tree with n nodes labeled from 0 to n - 1. You are given the integer n and a 2D integer array edges of length n - 1, where edges[i] = [ai, bi] indicates that there is an edge between nodes ai and bi in the tree.

You are also given a 0-indexed integer array values of length n, where values[i] is the value associated with the ith node, and an integer k.

A valid split of the tree is obtained by removing any set of edges, possibly empty, from the tree such that the resulting components all have values that are divisible by k, where the value of a connected component is the sum of the values of its nodes.

Return the maximum number of components in any valid split.

 Okay normal graph.Okay value[i] -> ith node . okay split . Hmm so how do we split? do we first find sum of every path? and store it somewhere how would that look like? Off the hook this sounds like a dp right? because we dont exactly know what criteria we will use to seperate so we might try to seperate nodes and calculate sum then?  Hmm but is it ? Lets think for a while. So we know we want maximum so we need to make as many splits as possible so we should take a running sum and see if the running sum % k == 0 make a split set runnign sum back to 0 . yes but how do we know that the split we made would also be divisble by k thats why tere might be two options make running sum 0 or dont make the split keep going right. what if we do a bfS? in the queue we can store : ok so first the parent node   n = 7, edges = [[0,1],[0,2],[1,3],[1,4],[2,5],[2,6]], values = [3,0,6,1,5,2,1], k = 3
 parent node is 0 whose calue is 3. so in queue we hac {0,3,false,false}->the two false is checking if we have cut the tree from left or right. because if we have we cannot go there. right. now the child of two but wait should we also store {0,3,true,false},{0,3,false,true},{0,3,true,true}? condition can be done only if value of 0 is divisible by k which it is but other condtion depend on children so initally our queue will have {0,3, false,false},{0,3,true,true} hmm but if we go in this direction it might work but its going to get complicated.so lets just keep sum in 0th level we have sum 3. can we split yes? hmm but how do we then calculate splits man. so do we do either add left sum to 3? or just add right sum to 3? or dont add any sum to 3 because its already dvisible byk? probably so .lets see children of 0 are 2 and 1 . values 6 and 0? add 3+6+0 = 9 add to queue 1 level -9 sum , but either only take left so 3+6=9; or only 3+ 0 =3 all look valid but split is differnt level 0 no solit level 1 .hmm but this is also getting complicated lets see the main greedy idea if(currentSum % k ==0) -> we can split.else not we can make 2 splits her right we pass 2 splits to children that hey your parent have already made two splits continue from there and see how many splits you make. hmm maybe lets change the approach how about we store the leaveas first and process from there? it would make it easy to split cause we wont have to worry about leaves much or nodes below it yeaa. but doing bfs would be lengthy lets try to do the same things with dfs. so what do we actually need to do with dfs? what kind of traversal maybe a post order traversal? yes because we want leaves to pass the sum up so that would be good . we would have a dfs function which would calu our subtree sum if summ satisfies the conditon increase count of components. but why and how do we know that doing this wuold make good component . meaning how do we know that if we split this subtree and increase component the other subtree would also be good. hmm its because if we get one valid split doesnt mean other split would be good so our component is only one the orignal tree but it doesnt feel right to do like this. ah okay got it so the solution just boil down to simple dfs and sum crazy.